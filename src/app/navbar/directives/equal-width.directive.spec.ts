import { EqualWidthDirective } from './equal-width.directive';

describe('EqualWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualWidthDirective();
    expect(directive).toBeTruthy();
  });
});
